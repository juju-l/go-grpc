syntax = "proto3";

import "result.proto"; // 导入 proto

option go_package = "gitee.com/vipex/go-grpc/gen";

package proto;

message User {
  string user = 1;
  string pswd = 2;
}

service UserGrpc {
  rpc Login (User) returns (BaseResult) {}
}
